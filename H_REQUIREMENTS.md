# H 题需求与实现差距清单

本文档用于把 `H题_车载平衡滚球运动控制系统.pdf` 的题目要求和当前 CCS 工程状态对应起来，避免后续开发只完成局部功能。

## 题目目标

制作一辆带车载平衡滚球运动控制装置的循线小车。小车沿黑色环形路线顺时针行驶，行驶过程中钢球需要在凹槽摆杆内保持在指定位置附近。

场地关键信息：

- 路面为白底黑线，黑线宽度 `1.8 +/- 0.2 cm`。
- 轨迹为环形路线，AB、CD 直线段均为 `1.5 m`，BC、DA 为半径 `0.5 m` 的半圆。
- A 点有启停线，并有 `30 cm` 停车基准虚线用于判断停车位置。
- 小车必须轮式驱动、车载电池供电，不得人工干预或遥控。
- 循迹模块只能使用红外光电模块，数量不限。
- 小车必须有启动按键和显示装置，按键启动时开始计时并显示时间，显示屏尺寸不大于 2 英寸。

## 评分要求

| 编号 | 要求 | 分值 | 当前状态 |
| --- | --- | ---: | --- |
| 1 | 安装钢球位置监测图传装置，实时显示、记录并回放钢球运动视频 | 6 | 未完成，需要摄像头、图传/显示/存储方案 |
| 2 | 小车从 A 点按键启动，顺时针循线一圈并停到 A 点；总时间 <= 20 s，停车误差 <= 2 cm，停止计时并显示 | 16 | 部分完成：已有按键、循迹、计时；未完成 A 点识别、一圈判定、精确停车和显示 |
| 3 | 静止状态下，摆杆控制钢球从 O 到 +5 cm，返回后到 -5 cm 并稳定；最大误差 <= 1 cm | 13 | 未完成，需要球位置检测和步进电机摆杆闭环 |
| 4 | 小车从 A 点启动通过 B 点；AB 时间 <= 8 s，钢球稳定在 O 附近，误差 <= 1 cm | 20 | 未完成，需要 B 点/路段识别、车运动时滚球闭环 |
| 5 | 小车从 A 点启动顺时针一圈通过 A；总时间 <= 30 s，钢球稳定在 O 附近，误差 <= 1 cm | 20 | 未完成，需要整圈运动流程和滚球闭环 |
| 6 | 钢球在任意指定位置，小车顺时针一圈通过 A；总时间 <= 30 s，钢球稳定在指定位置附近，误差 <= 1 cm | 20 | 未完成，需要指定位置输入、球位置闭环和整圈流程 |
| 7 | 其他 | 5 | 未整理 |
| 8 | 设计报告 | 20 | 未完成，需要方案、理论、硬件、程序流程和测试数据 |

## 当前已实现

- 后驱二轮小车硬件映射已在 SysConfig 和驱动代码中配置。
- B15 作为启动按键，B5 作为停止按键。
- UART1 使用中断接收命令，可串口调节速度闭环和循迹参数。
- 8 路灰度传感器已接入并用于循迹。
- 左右轮编码器已用于速度闭环。
- 运行计时采用 `RUN_TIMER` 的 1 ms 硬件定时中断，停止时通过 UART 输出 `RUN TIME MS`。
- UART2 已作为备用串口配置。
- OLED I2C 引脚已配置，但运行时显示暂时关闭，原因是当前 OLED 模块已确认损坏。
- 步进电机控制脚和 PA30 PWM 已预留，但未接入闭环控制。

## 当前默认控制参数

代码位置：[app_config.h](app_config.h)

| 参数 | 当前值 | 作用 |
| --- | ---: | --- |
| `LINE_DEFAULT_TARGET` | `120.0` | 速度闭环目标编码器计数 |
| `LINE_DEFAULT_BASE_PWM` | `255` | 前进基础 PWM |
| `DEFAULT_KP` | `1.20` | 左右轮速度闭环 P |
| `DEFAULT_KI` | `0.003` | 左右轮速度闭环 I |
| `DEFAULT_KD` | `0.0` | 左右轮速度闭环 D |
| `LINE_DEFAULT_KP` | `2.45` | 循迹位置误差 P |
| `LINE_DEFAULT_KD` | `1.00` | 循迹位置误差 D |
| `LINE_CORR_LIMIT` | `200` | 循迹差速最大修正量 |
| `DEFAULT_RIGHT_PWM_TRIM` | `-14` | 右轮 PWM 补偿 |

## 后续路线

1. 稳定小车底盘：继续解决上电偶发跑动，保留当前响应较好的按键处理。
2. 完成题目第 2 项：用启停线/灰度传感器实现 A 点识别、圈数判断、停车制动和时间显示。
3. 恢复显示功能：更换可用 OLED 后开启 `OLED_RUNTIME_ENABLE`，显示运行时间和模式。
4. 加入滚球位置检测：按题目要求使用摄像头检测凹槽内钢球位置，并输出位置误差。
5. 实现摆杆步进电机闭环：建议采用外环球位置 PID、内环摆杆角度/步频控制，启动和停车时叠加小车加速度前馈。
6. 完成第 3、4、5、6 项测试模式：静止滚球、A 到 B、整圈 O 点稳定、整圈指定点稳定。
7. 整理设计报告：包含方案论证、循迹理论、摆杆控制理论、电路与程序流程、测试数据和误差分析。
